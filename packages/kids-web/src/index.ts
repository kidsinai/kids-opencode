// @kidsinai/kids-web
//
// Phase 3 deliverable (W5-6). Vite + React + Tailwind + Monaco scaffold pending.
//
// Layout (per kids-opencode-spec.md §4.1):
//   Left:   project tree (files only, paths hidden)
//   Center: Monaco editor + diff overlay
//   Right:  agent dialog (plan → tool call → result), always-visible
//           "同意 / 修改 / 取消" buttons, Stars estimate badge

export const WEB_VERSION = "0.0.0";
