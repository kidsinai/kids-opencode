{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@kidsinai/kids-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Kid-facing React web app: project tree + Monaco editor + agent dialog. Talks to opencode server via @opencode-ai/sdk.",
  "license": "MIT",
  "scripts": {
    "dev": "echo 'Phase 3 deliverable (W5-6). Vite + React scaffold pending.'",
    "build": "echo 'Phase 3 deliverable.'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@opencode-ai/sdk": "^1.14.50"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
